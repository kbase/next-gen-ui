# Used when no REGISTRY_UPSTREAM is configured: the host sees an empty registry
# and runs with its bundled plugins.
location = /plugin-registry/plugins {
  default_type application/json;
  add_header Cache-Control "no-cache" always;
  return 200 '[]';
}
